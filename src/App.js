import React from 'react';
import $ from 'jquery';
import Markdown from './Pages/Markdown'

class App extends React.Component {
    render() {
        return (
            <Markdown />
        )
    }
};

export default App;
