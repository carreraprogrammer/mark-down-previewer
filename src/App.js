import React from 'react';
import Markdown from './Pages/Markdown'

class App extends React.Component {
    render() {
        return (
            <Markdown />
        )
    }
};

export default App;
